# Keystore Properties para Udar Edge
# 
# INSTRUCCIONES:
# 1. Copia este archivo a: android/keystore.properties
# 2. Reemplaza los valores con tus credenciales reales
# 3. ⚠️ NUNCA subas este archivo a Git (debe estar en .gitignore)
# 
# Para generar el keystore, ejecuta:
# keytool -genkey -v -keystore android/keystores/udar-edge-release.keystore \
#   -alias udar-edge -keyalg RSA -keysize 2048 -validity 10000

# Ruta al archivo keystore (relativa a android/)
storeFile=keystores/udar-edge-release.keystore

# Contraseña del keystore
# ⚠️ GUARDA ESTA CONTRASEÑA EN LUGAR SEGURO (1Password, AWS Secrets, etc.)
storePassword=TU_PASSWORD_AQUI

# Alias de la clave (el nombre que le diste al crear el keystore)
keyAlias=udar-edge

# Contraseña de la clave
# (Puede ser la misma que storePassword si así lo configuraste)
keyPassword=TU_PASSWORD_AQUI
